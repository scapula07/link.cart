import { PaymentTypes } from '@requestnetwork/types';
import { IPaymentRetriever } from '../types';
/**
 * Retrieves a list of transfer events for an address
 */
export default class ERC20InfoRetriever implements IPaymentRetriever<PaymentTypes.ERC20PaymentNetworkEvent> {
    private tokenContractAddress;
    private toAddress;
    private eventName;
    private network;
    /**
     * @param tokenContractAddress The address of the ERC20 contract
     * @param toAddress Address of the balance we want to check
     * @param eventName Indicate if it is an address for payment or refund
     * @param network The Ethereum network to use
     */
    constructor(tokenContractAddress: string, toAddress: string, eventName: PaymentTypes.EVENTS_NAMES, network: string);
    /**
     * Retrieves transfer events for the current contract, address and network.
     */
    getTransferEvents(): Promise<PaymentTypes.ERC20PaymentNetworkEvent[]>;
}
//# sourceMappingURL=address-based-info-retriever.d.ts.map