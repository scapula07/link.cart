import { PaymentTypes } from '@requestnetwork/types';
import { IPaymentRetriever } from '../types';
/**
 * Gets a list of transfer events for an address and payment reference
 */
export declare class EthInputDataInfoRetriever implements IPaymentRetriever<PaymentTypes.ETHPaymentNetworkEvent> {
    private toAddress;
    private eventName;
    private network;
    private paymentReference;
    private explorerApiKey?;
    /**
     * @param toAddress Address to check
     * @param eventName Indicate if it is an address for payment or refund
     * @param network The id of network we want to check
     * @param paymentReference The reference to identify the payment
     * @param explorerApiKey The explorer (etherscan...) API key
     */
    constructor(toAddress: string, eventName: PaymentTypes.EVENTS_NAMES, network: string, paymentReference: string, explorerApiKey?: string | undefined);
    getTransferEvents(): Promise<PaymentTypes.ETHPaymentNetworkEvent[]>;
}
//# sourceMappingURL=info-retriever.d.ts.map