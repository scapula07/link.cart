import { IdentityTypes } from '@requestnetwork/types';
declare const _default: {
    areEqual: typeof areEqual;
    hasError: typeof hasError;
    normalizeIdentityValue: typeof normalizeIdentityValue;
    supportedIdentities: IdentityTypes.TYPE[];
};
/**
 * Module to manage Request Logic Identity
 */
export default _default;
/**
 * Checks if two identities are equals
 *
 * @param IIdentity id1 first identity
 * @param IIdentity id2 second identity
 * @returns boolean
 */
declare function areEqual(id1?: IdentityTypes.IIdentity, id2?: IdentityTypes.IIdentity): boolean;
/**
 * Normalizes identity values
 *
 * @param string value value to normalize
 * @returns string value normalized
 */
declare function normalizeIdentityValue(value: string): string;
/**
 * Checks if the identity has an error
 *
 * @param id identity to check
 * @returns the error or null if valid
 */
declare function hasError(id: IdentityTypes.IIdentity): string | null;
//# sourceMappingURL=identity.d.ts.map