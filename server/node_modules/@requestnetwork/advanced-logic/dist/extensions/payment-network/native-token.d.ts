import { CurrencyTypes, ExtensionTypes } from '@requestnetwork/types';
import ReferenceBasedPaymentNetwork from './reference-based';
/**
 * Implementation of the payment network to pay in ETH based on input data.
 */
export default abstract class NativeTokenPaymentNetwork extends ReferenceBasedPaymentNetwork {
    readonly supportedNetworks: CurrencyTypes.ChainName[];
    constructor(extensionId: ExtensionTypes.PAYMENT_NETWORK_ID, currentVersion: string, supportedNetworks: CurrencyTypes.ChainName[]);
    createCreationAction(creationParameters: ExtensionTypes.PnReferenceBased.ICreationParameters): ExtensionTypes.IAction<ExtensionTypes.PnReferenceBased.ICreationParameters>;
    protected isValidAddress(_address: string): boolean;
    protected throwIfInvalidNetwork(network?: CurrencyTypes.ChainName): asserts network is CurrencyTypes.ChainName;
}
//# sourceMappingURL=native-token.d.ts.map