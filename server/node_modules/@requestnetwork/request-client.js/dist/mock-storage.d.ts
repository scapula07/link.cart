import { StorageTypes } from '@requestnetwork/types';
/**
 * Storage layer implemented with in-memory hashmap, to be used for testing.
 */
export default class MockStorage implements StorageTypes.IStorage {
    private data;
    private forceEmitError;
    initialize(): Promise<void>;
    ipfsAdd(content: string): Promise<StorageTypes.IIpfsMeta>;
    append(content: string): Promise<StorageTypes.IAppendResult>;
    read(id: string): Promise<StorageTypes.IEntry>;
    readMany(ids: string[]): Promise<StorageTypes.IEntry[]>;
    getData(): Promise<StorageTypes.IEntriesWithLastTimestamp>;
    getIgnoredData(): Promise<StorageTypes.IEntry[]>;
    /**
     * Gets information
     *
     * @param detailed if true get the list of files hash
     */
    _getStatus(detailed?: boolean): Promise<any>;
    _makeNextAppendFailInsteadOfConfirmed(): void;
}
//# sourceMappingURL=mock-storage.d.ts.map