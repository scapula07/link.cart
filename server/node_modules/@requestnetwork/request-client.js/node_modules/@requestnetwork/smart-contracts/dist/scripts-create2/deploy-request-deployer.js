"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.deployDeployer = void 0;
const tslib_1 = require("tslib");
const verify_1 = require("./verify");
// Deploys, set up the contracts
function deployDeployer(hre) {
    return tslib_1.__awaiter(this, void 0, void 0, function* () {
        try {
            const [deployer] = yield hre.ethers.getSigners();
            // Deploy the contract RequestDeployer
            const RequestDeployer__factory = yield hre.ethers.getContractFactory('RequestDeployer', deployer);
            const RequestDeployer = yield RequestDeployer__factory.deploy();
            yield (0, verify_1.verifyOne)(RequestDeployer.address, { contract: 'RequestDeployer' }, hre);
            // ----------------------------------
            console.log('Contract deployed');
            console.log(`
      RequestDeployer:        ${RequestDeployer.address}
      Block number:           ${RequestDeployer.deployTransaction.blockNumber}
      `);
        }
        catch (e) {
            console.error(e);
        }
    });
}
exports.deployDeployer = deployDeployer;
//# sourceMappingURL=deploy-request-deployer.js.map