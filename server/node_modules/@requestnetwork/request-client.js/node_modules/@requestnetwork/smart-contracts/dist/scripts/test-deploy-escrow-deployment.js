"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.deployEscrow = void 0;
const tslib_1 = require("tslib");
require("@nomiclabs/hardhat-ethers");
const deploy_one_1 = require("../scripts/deploy-one");
// Deploys, set up the contracts
function deployEscrow(hre) {
    return tslib_1.__awaiter(this, void 0, void 0, function* () {
        const erc20FeeProxyAddress = '0x75c35C980C0d37ef46DF04d31A140b65503c0eEd';
        let deployer;
        try {
            [deployer] = yield hre.ethers.getSigners();
            const deployerAddress = yield deployer.getAddress();
            // Deploy Escrow contract
            const { address: erc20EscrowToPayAddress } = yield (0, deploy_one_1.deployOne)({}, hre, 'ERC20EscrowToPay', {
                constructorArguments: [erc20FeeProxyAddress, deployerAddress],
            });
            // ----------------------------------
            console.log('Contracts deployed');
            console.log(`
      ERC20EscrowToPay:         ${erc20EscrowToPayAddress}
    `);
        }
        catch (e) {
            console.error(e);
        }
    });
}
exports.deployEscrow = deployEscrow;
//# sourceMappingURL=test-deploy-escrow-deployment.js.map