import { TransactionRequest } from '../../../common';
import { ExternalContract } from "../external.contract";
export declare class SuperTokenContract extends ExternalContract {
    private _address;
    constructor(address: string);
    encodeUpgrade?(amount: any): TransactionRequest;
    get address(): string;
}
