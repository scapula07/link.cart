import { BigNumber } from 'ethers';
import { BaseClass } from '../../common';
export declare class CrossChainBridgeChainCurrency extends BaseClass<CrossChainBridgeChainCurrency> {
    address: string;
    icon: string;
    name: string;
    symbol: string;
    decimals: number;
    minNativeCurrencyForGas: BigNumber;
}
