import { Service } from '../common';
import { NameResolutionsNodes } from './classes';
export declare class NameResolutionService extends Service {
    resolveName(chainId: number, name: string): Promise<NameResolutionsNodes>;
}
