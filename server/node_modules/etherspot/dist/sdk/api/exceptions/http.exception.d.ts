import { Exception } from '../../common';
import { HttpExceptionCodes } from './constants';
export declare class HttpException extends Exception {
    code: HttpExceptionCodes;
    constructor(code: HttpExceptionCodes, message?: string);
}
