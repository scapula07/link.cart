import { StoredSession, SessionStorageLike } from './interfaces';
export declare class SessionStorage implements SessionStorageLike {
    private data;
    setSession(walletAddress: string, session: StoredSession): Promise<void>;
    getSession(walletAddress: string): Promise<StoredSession>;
}
