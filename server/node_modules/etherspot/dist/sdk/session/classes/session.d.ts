import { Account } from '../../account';
import { BaseClass } from '../../common';
import { StoredSession } from '../interfaces';
export declare class Session extends BaseClass<Session> {
    static fromStoredSession(storedSession: StoredSession<Date | string | number>): Session;
    static TTL_MARGIN: number;
    token: string;
    ttl: number;
    account?: Account;
    expireAt?: Date;
    refresh?(): void;
    verify?(): boolean;
    toStoredSession?(): StoredSession;
}
