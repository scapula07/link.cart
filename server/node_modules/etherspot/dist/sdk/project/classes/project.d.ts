import { Synchronized } from '../../common/classes';
import { ProjectStates } from '../constants';
export declare class Project extends Synchronized {
    key: string;
    address: string;
    state: ProjectStates;
    createdAt: Date;
    updatedAt: Date;
}
