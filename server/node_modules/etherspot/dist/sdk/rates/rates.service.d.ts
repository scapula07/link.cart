import { Service } from '../common';
import { RateData } from './classes';
export declare class RatesService extends Service {
    fetchExchangeRates(tokens: string[], ChainId: number): Promise<RateData>;
}
